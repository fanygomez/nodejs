# Notas:
Practicas nodejs

## tabla-consola/
```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     base de la tabla a generar                  [number] [required]
  -l, --listar   Bandera para mostrar el resultado de la tabla en consola
                                                      [boolean] [default: false]
  -h, --hasta    N hasta el numero que desees genear la tabla
                                                          [number] [default: 10]
```